import { isAdminAuthenticated } from "@/lib/auth";
import { prisma } from "@/lib/prisma";
export async function POST(request: Request, { params }: { params: Promise<{ id: string }> }) { if (!await isAdminAuthenticated()) return Response.json({ error: "Neautorizovaný přístup." }, { status: 401 }); const bettingRoundId = Number((await params).id); const body = await request.json().catch(() => ({})); const winnerPlayerIds = Array.isArray(body.winnerPlayerIds) ? body.winnerPlayerIds.map(Number).filter(Number.isInteger) : []; if (!winnerPlayerIds.length) return Response.json({ error: "Vyber alespoň jednoho vítěze." }, { status: 400 }); const result = await prisma.$transaction(async (tx) => { const round = await tx.bettingRound.update({ where: { id: bettingRoundId }, data: { isOpen: false, isResolved: true, resolvedAt: new Date(), winners: { create: winnerPlayerIds.map((playerId: number) => ({ playerId })) } } }); const winningBets = await tx.bet.findMany({ where: { bettingRoundId, candidateId: { in: winnerPlayerIds } } }); for (const bet of winningBets) { await tx.player.update({ where: { id: bet.bettorId }, data: { points: { increment: round.rewardPoints } } }); await tx.pointsLedgerEntry.create({ data: { playerId: bet.bettorId, delta: round.rewardPoints, source: "BETTING_WIN", note: round.title ?? "Výhra v sázce" } }); } return { round, winnersPaid: winningBets.length }; }); return Response.json(result); }
